apiVersion: v1
kind: Service
metadata:
  name: kubebox
  labels:
    app: "kubebox"
spec:
  type: NodePort
  ports:
  - name: http
    nodePort: 30092
    protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: "kubebox"
